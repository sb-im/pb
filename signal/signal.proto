syntax = "proto3";
package pb;

option go_package = "github.com/SB-IM/pb/signal";

message SessionDescription {
    int32 type= 1;
    bytes description = 2;
    string id = 3;
    TrackSource track_source = 4;
}

enum TrackSource {
    DRONE = 0;
    MONITOR = 1;
}
